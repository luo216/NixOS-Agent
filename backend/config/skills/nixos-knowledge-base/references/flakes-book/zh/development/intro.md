# intro

This document was split automatically because it exceeded `300` lines.

- source_lines: `472`
- split_method: `headings-h2`
- part_count: `2`

## Parts

- `通过 `nix shell` 创建开发环境 / 创建与使用开发环境 / 在开发环境中使用 zsh/fish 等其他 shell / 通过 `pkgs.runCommand` 创建开发环境` -> `intro.parts/part-001-nix-shell-zsh-fish-shell-pkgs-runcommand.md` (274 lines)
- `进入任何 Nix 包的构建环境 / `nix build` / 使用 `nix profile` 分别管理日常娱乐环境跟开发环境 / 其他命令 / References` -> `intro.parts/part-002-nix-nix-build-nix-profile-references.md` (197 lines)
