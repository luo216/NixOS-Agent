---
module: services.kanidm.provision.systems.oauth2.<name>
option_count: 15
source: options.html
---

# services.kanidm.provision.systems.oauth2.<name>

## services.kanidm.provision.systems.oauth2.<name>.enableLegacyCrypto

Enable legacy crypto on this client. Allows JWT signing algorthms like RS256. Type: boolean Default: false Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.enableLocalhostRedirects

Allow localhost redirects. Only for public clients. Type: boolean Default: false Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.allowInsecureClientDisablePkce

Disable PKCE on this oauth2 resource server to work around insecure clients that may not support it. You should request the client to enable PKCE! Only for non-public clients. Type: boolean Default: false Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.basicSecretFile

The basic secret to use for this service. If null, the random secret generated by kanidm will not be touched. Do NOT use a path from the nix store here! Type: null or absolute path Default: null Example: "/run/secrets/some-oauth2-basic-secret" Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.claimMaps

Adds additional claims (and values) based on which kanidm groups an authenticating party belongs to. See Claim Maps for more information. Type: attribute set of (submodule) Default: { } Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.displayName

Display name Type: string Example: "Some Service" Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.imageFile

Application image to display in the WebUI. Kanidm supports “image/jpeg”, “image/png”, “image/gif”, “image/svg+xml”, and “image/webp”. The image will be uploaded each time kanidm-provision is run. Type: null or absolute path Default: null Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.originLanding

When redirecting from the Kanidm Apps Listing page, some linked applications may need to land on a specific page to trigger oauth2/oidc interactions. Type: string Example: "https://someservice.example.com/home" Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.originUrl

The redirect URL of the service. These need to exactly match the OAuth2 redirect target Type: string matching the pattern .*://?.*$ or non-empty (list of string matching the pattern .*://?.*$) Example: "https://someservice.example.com/auth/login" Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.preferShortUsername

Use ‘name’ instead of ‘spn’ in the preferred_username claim Type: boolean Default: false Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.present

Whether to ensure that this oauth2 resource server is present or absent. Type: boolean Default: true Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.public

Whether this is a public client (enforces PKCE, doesn’t use a basic secret) Type: boolean Default: false Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.removeOrphanedClaimMaps

Whether claim maps not specified here but present in kanidm should be removed from kanidm. Type: boolean Default: true Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.scopeMaps

Maps kanidm groups to returned oauth scopes. See Scope Relations for more information. Type: attribute set of list of string Default: { } Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>

## services.kanidm.provision.systems.oauth2.<name>.supplementaryScopeMaps

Maps kanidm groups to additionally returned oauth scopes. See Scope Relations for more information. Type: attribute set of list of string Default: { } Declared by: <nixpkgs/nixos/modules/services/security/kanidm.nix>
