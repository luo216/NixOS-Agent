---
module: services.tinc
option_count: 1
source: options.html
---

# services.tinc

## services.tinc.networks

Defines the tinc networks which will be started. Each network invokes a different daemon. Type: attribute set of (submodule) Default: { } Declared by: <nixpkgs/nixos/modules/services/networking/tinc.nix>
