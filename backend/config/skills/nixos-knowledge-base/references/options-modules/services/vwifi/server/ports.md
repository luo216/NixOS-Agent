---
module: services.vwifi.server.ports
option_count: 4
source: options.html
---

# services.vwifi.server.ports

## services.vwifi.server.ports.control

The control interface port. Set to null if we should leave it unset. Type: null or 16 bit unsigned integer; between 0 and 65535 (both inclusive) Default: null Declared by: <nixpkgs/nixos/modules/services/networking/vwifi.nix>

## services.vwifi.server.ports.spy

The spy interface port. Set to null if we should leave it unset. Type: null or 16 bit unsigned integer; between 0 and 65535 (both inclusive) Default: null Declared by: <nixpkgs/nixos/modules/services/networking/vwifi.nix>

## services.vwifi.server.ports.tcp

The TCP server port. Set to null if we should leave it unset. Type: null or 16 bit unsigned integer; between 0 and 65535 (both inclusive) Default: null Declared by: <nixpkgs/nixos/modules/services/networking/vwifi.nix>

## services.vwifi.server.ports.vhost

The vhost port. Set to null if we should leave it unset. Type: null or 16 bit unsigned integer; between 0 and 65535 (both inclusive) Default: null Declared by: <nixpkgs/nixos/modules/services/networking/vwifi.nix>
