---
module: services.glusterfs.tlsSettings
option_count: 3
source: options.html
---

# services.glusterfs.tlsSettings

## services.glusterfs.tlsSettings.caCert

Path certificate authority used to sign the cluster certificates. Type: absolute path Declared by: <nixpkgs/nixos/modules/services/network-filesystems/glusterfs.nix>

## services.glusterfs.tlsSettings.tlsKeyPath

Path to the private key used for TLS. Type: string Declared by: <nixpkgs/nixos/modules/services/network-filesystems/glusterfs.nix>

## services.glusterfs.tlsSettings.tlsPem

Path to the certificate used for TLS. Type: absolute path Declared by: <nixpkgs/nixos/modules/services/network-filesystems/glusterfs.nix>
